name: bankapp
prefect-version: 3.2.2

build: null

push: null

pull:
  - prefect.deployments.steps.set_working_directory:
      directory: /home/mt24056/Documents/prefect/bankapp

deployments:
- name: banking-deployment
  version: "1.0"
  tags: ["banking"]
  description: "A simple banking flow"
  entrypoint: bankapp.py:banking_flow
  parameters: {}
  schedule:
    - cron: "*/5 * * * *"
      timezone: "Asia/Kolkata"
      active: true
  work_pool:
    name: default-use
    type: Prefect Managed
    job_variables: {}